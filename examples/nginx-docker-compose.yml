version: "3.8"
services:
  web:
    image: nginx:alpine
    command: ["nginx", "-g", "daemon off;"]
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./config.json:/app/config.json
